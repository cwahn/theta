[package]
name = "web-chat-browser"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
web-chat-shared = { path = "../shared", features = ["ts", "remote"] }
theta-wasm = { path = "../../../theta-wasm", features = ["remote"] }
wasm-bindgen = "0.2"
